{{- if .Values.secrets.createDefaultSecret }}
# WARNING: Placeholder secret for dev only. In production use existingSecret with real values.
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "forgeportal.fullname" . }}-secret
  labels:
    {{- include "forgeportal.labels" . | nindent 4 }}
type: Opaque
stringData:
  db-password: "change-me-dev-only"
  oidc-client-secret: "change-me-dev-only"
  encryption-key: "change-me-dev-only-32chars!!"
  session-secret: "change-me-forgeportal-session-secret"
{{- end }}
